name: centrale-ni-front

services:
  front:
    image: ${FRONT_IMAGE:?renseigner FRONT_IMAGE}
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:?renseigner NEXT_PUBLIC_API_URL}
    ports:
      - '127.0.0.1:${FRONT_PORT:-40510}:40510'
